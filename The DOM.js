// DOM(Document object model) - Created by the browser
// Def. : When the browser parses an HTML document. It creates a representation in memory of all the elements on the page. This representation is called 'DOM'. It's a tree structure where each element represent by a 'node'.
console.log(document.location);
console.log(document.URL);